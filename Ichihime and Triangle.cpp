#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int tCase,a,b,c,d;

    cin >> tCase ;

    while(tCase--)
    {
        cin >> a >> b >> c >> d ;
        cout << b << " " << c << " " << c << "\n" ;

    }

    return 0 ;
}
